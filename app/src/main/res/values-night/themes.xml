<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Stargazers" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="star">
        <item name="android:layout_height">@dimen/star_size</item>
        <item name="android:layout_width">@dimen/star_size</item>
    </style>

    <style name="moon">
        <item name="android:src">@drawable/moon</item>
        <item name="android:layout_height">@dimen/moon_size</item>
        <item name="android:layout_width">@dimen/moon_size</item>
    </style>

    <style name="error_image">
        <item name="android:src">@drawable/error</item>
        <item name="android:layout_height">@dimen/error_size</item>
        <item name="android:layout_width">@dimen/error_size</item>
        <item name="android:layout_marginTop">@dimen/twentyfour</item>
    </style>

    <style name="drawable_dialog_bg">
        <item name="android:background">@drawable/dialog_bg</item>
    </style>

</resources>